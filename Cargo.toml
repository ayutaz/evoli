[package]
name = "evolution-island"
version = "0.3.0"
edition = "2021"

[dependencies]
bevy = "0.15"
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
ron = "0.8"

[features]
profiler = []
perception_debug = []
